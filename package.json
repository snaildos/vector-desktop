{
  "name": "Vector-Desktop",
  "version": "1.0.0",
  "description": "Vector Desktop allows you too control your vector over the internet.",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/snaildos/vector-desktop.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --linux AppImage --publish never",
    "deploy": "electron-builder build --linux AppImage --publish always"
  },
  "author": "SnailDOS",
  "license": "ISC",
  "build": {
    "remoteBuild": "false"
  },
  "devDependencies": {
    "electron": "5.0.13",
    "electron-builder": "20.44.4"
  },
  "dependencies": {
    "electron-updater": "^4.0.6",
    "electron-is": "^3.0.0"
  }
}
